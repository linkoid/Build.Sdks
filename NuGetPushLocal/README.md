# NuGet Push Local
Pushes a package to the local feed on pack.

# Version 2.0
Version 2.0 uses the dotnet CLI instead of the nuget CLI by default.
Usage of the nuget CLI can be renabled via `<NuGetPushLocalUseNugetCli>true</NuGetPushLocalUseNugetCli>`.
